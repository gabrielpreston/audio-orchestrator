# Include base requirements
-r ../requirements-base.txt

# Discord-specific dependencies
discord.py[voice]==2.5.0
discord-ext-voice_recv==0.5.2a179
numpy==1.26.0
PyNaCl==1.5.0
openwakeword==0.6.0
rapidfuzz==3.5.1
webrtcvad==2.0.10

# Openwakeword dependencies (onnxruntime required for ONNX inference on Linux)
# Note: tflite-runtime is unavailable on Linux, so openwakeword is installed with --no-deps in Dockerfile
onnxruntime>=1.16.0,<2.0
