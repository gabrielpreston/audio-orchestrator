"""Integration tests for audio-orchestrator services.

This package contains integration tests that validate service-to-service
communication via HTTP boundaries using real Docker Compose services.

All tests use standardized {SERVICE}_BASE_URL environment variables with
agnostic service names (not implementation-specific).
"""
