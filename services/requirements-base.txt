# Base requirements shared across all services
# Core web framework
fastapi>=0.118,<1.0
uvicorn[standard]>=0.37,<1.0
pydantic>=2.12,<3.0

# Logging
structlog>=24.1,<25.0

# HTTP client
httpx>=0.26,<1.0

# NOTE: The following dependencies are now included in the python-ml base image:
# - PyTorch (torch, torchvision, torchaudio)
# - OpenTelemetry (opentelemetry-api, opentelemetry-sdk, instrumentation packages, exporters)
# - gRPC (grpcio, grpcio-tools, protobuf)
# - ML Libraries (transformers, accelerate, safetensors, faster-whisper, speechbrain)
# - Audio Libraries (librosa, soundfile, scipy, numpy, bark)
#
# Individual services should NOT reinstall these dependencies unless they need
# specific versions different from the base image.
