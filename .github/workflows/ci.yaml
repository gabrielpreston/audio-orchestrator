# DEPRECATED: This workflow has been replaced by the multi-workflow architecture
# New workflows: main-ci.yaml, core-ci.yaml, docker-ci.yaml, docs-ci.yaml, security-ci.yaml
# This file is kept for reference only and should not be modified
# TODO: Remove after confirming new workflows work correctly

# Workflow: CI
# Purpose: Run linting, testing, security scanning, and Docker smoke tests
# when code changes are detected.
name: "CI"
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch: {}
permissions:
  contents: "read"
  pull-requests: "read"
  packages: "write" # Required for GHCR publishing

concurrency:
  group: "ci-${{ github.ref }}"
  cancel-in-progress: true

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_PYTHON_VERSION_WARNING: "1"
  PYTHONDWRITEBYTECODE: "1"

jobs:
  # DISABLED: This workflow is deprecated in favor of the new multi-workflow architecture
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: "Workflow disabled"
        run: echo "This workflow has been replaced by main-ci.yaml and specialized workflows"
