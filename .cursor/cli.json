{
  "permissions": {
    "allow": [
      "Shell(make test)",
      "Shell(make lint)",
      "Shell(make run)",
      "Shell(make stop)",
      "Shell(make lint-fix)",
      "Shell(make logs)",
      "Shell(make docker-build)",
      "Shell(make docker-clean)",
      "Shell(make validate-changes)",
      "Shell(make check-syntax)",
      "Shell(make security)",
      "Shell(make restart)",
      "Shell(source .venv/bin/activate && git commit)",
      "Shell(source .venv/bin/activate && python)",
      "Shell(source .venv/bin/activate && python3)"
    ],
    "deny": []
  }
}
